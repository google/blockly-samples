const assert = require('assert');
const Blockly = require('blockly');

require('../dist/plus-minus-mutators.umd');

suite('If Blocks', () => {
  test('test test', () => {

  });
  test('fail test', () => {
    chai.assert.fail();
  });
});
